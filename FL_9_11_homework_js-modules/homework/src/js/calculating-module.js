export const add = (n1, n2) => n1 + n2;
export const sub = (n1, n2) => n1 - n2;
export const mult = (n1, n2) => n1 * n2;
export const divide = (n1, n2) => n1 / n2;
export const pow = (x, y) => Math.pow(x, y);
export const root = (x, n) => Math.pow(x, 1/n);
